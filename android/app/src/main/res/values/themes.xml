<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme — Compose handles all theming, this is just for window/status bar -->
    <style name="Theme.Sakhi" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:navigationBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <!-- Splash screen theme — shows while app initializes -->
    <style name="Theme.Sakhi.Splash" parent="Theme.Sakhi">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
